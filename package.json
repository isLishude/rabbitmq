{
  "name": "rabbitmq-service",
  "version": "0.0.2",
  "description": "rabbitmq service",
  "main": "dist/index.js",
  "types": "types",
  "scripts": {
    "build": "rm -rf dist types && tsc",
    "pub": "git push --all && git push --tags && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/isLishude/rabbitmq.git"
  },
  "author": "lishude",
  "license": "MIT",
  "devDependencies": {
    "@types/amqplib": "^0.5.7",
    "@types/node": "^10.1.0",
    "tslint": "^5.10.0",
    "tslint-config-prettier": "^1.12.0",
    "typescript": "^2.8.3"
  },
  "dependencies": {
    "amqplib": "^0.5.2"
  }
}
